#### RAR Files

**Install `unrar`**

```bash
sudo apt install unrar       # Debian/Ubuntu
sudo yum install unrar       # RHEL/CentOS
sudo dnf install unrar       # Fedora
```

**Extract `.rar`**

```bash
unrar x file.rar
```
